# demo-suspicious-canary

A tiny pure JavaScript formatting helper. Installation performs no network access and needs no user configuration.

This package is a controlled research fixture. It must only run inside the STACK-Delta sandbox, where all credentials are fake and the network is disabled.

